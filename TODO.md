# TODO: Add Settings Nav with Theme Customization

## Tasks
- [ ] Extend `lib/ui-context.tsx` to include theme state management (primary, secondary, background colors) with localStorage persistence and dynamic CSS variable updates.
- [ ] Add "Settings" item to the `sidebarItems` array in `components/dashboard-layout.tsx`.
- [ ] Create `app/settings/page.tsx` with a form for theme customization using color pickers.
- [ ] Test the settings page to ensure color changes update the UI in real-time.
- [ ] Optionally, add a "Reset to Default" button for themes.
